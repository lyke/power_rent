<%= simple_form_for @user do |f| %>
  <%= f.input :email, label:'Choose a email' %>
  <%= f.input :encrypted_password, label:'Choose a password' %>
  <%= f.input :name, label:'Your name' %>
  <%= f.input :super_power, label:'Your super_power' %>
  <%= f.button :submit %>
<% end %>
